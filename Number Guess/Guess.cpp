#include <stdio.h>




int main(int argc, char ** argv) {

	//DToDo Loop while we have not guessed number
		//ToDo Make up current guess, half way between low and high
		//ToDo Show current guess
		//ToDo Get User Input (correct, high, low)
		//ToDo If correct Break loop
		//ToDo If too low set new High limit
		//ToDo If too high set new Low limit

	return	0;
}